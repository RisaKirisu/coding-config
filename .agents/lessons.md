# Lessons

- Keep responses concise and lead with the conclusion; avoid unnecessary walls of text.
- Treat the workspace shell as running inside the DevVM unless runtime evidence proves otherwise; failure to reach a host service does not show that the host service is stopped.
